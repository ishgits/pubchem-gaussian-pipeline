# Pinned dependency stack targeted for the v2.0.0 release.
#
# RDKit conformer generation is version-sensitive, so the release pins the
# stack. These exact dependency pins were exercised on 2026-07-13 with CPython
# 3.12.13; the verified results are recorded in
# docs/implementation-status.md. The clean-source-archive repeat also passed on
# 2026-07-13.
#
# Reproduce with: python -m pip install -r requirements-lock.txt
# numpy is intentionally not pinned as a direct dependency; it is pulled in
# transitively by pandas and rdkit.
#
# Supported Python: >=3.11.

pandas==3.0.3
requests==2.34.2
rdkit==2025.09.3
pytest==9.1.1
